# Simpro API Configuration
# Copy this file to .env.local for local development
# In Railway, set these as environment variables

SIMPRO_API_URL=https://your-subdomain.simprosuite.com/api/v1.0
SIMPRO_API_TOKEN=your_api_token_here
SIMPRO_COMPANY_ID=0

# Redis Configuration (optional - for customer count caching)
# If not set, will use in-memory cache with 1-hour TTL
# Example: redis://default:password@host:port
REDIS_URL=

# Base URL for the application (used for initialization)
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000
